apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-autopro.fullname" . }}-test-connection"
  labels:
    {{- include "go-autopro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-autopro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
