apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-autopro.fullname" . }}
  labels:
    {{- include "go-autopro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-autopro.selectorLabels" . | nindent 4 }}
